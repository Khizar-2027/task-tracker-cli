# Task Tracker CLI 📝

A simple command-line Task Tracker application built with Python.
This project allows users to add, view, update, and delete tasks with
persistent storage using a JSON file.

## 🚀 Features
- Add new tasks with description and status
- View all tasks
- Update task description and status
- Delete tasks
- Task status validation (todo, in-progress, done)
- Automatic timestamps (createdAt, updatedAt)
- Data persistence using a JSON file

## 🛠 Tech Stack
- Python
- JSON (for storage)
- datetime module

## ▶️ How to Run

1. Clone the repository:
git clone <your-repo-url>

2. Navigate to the project directory:
cd task-tracker

3. Run the program:
python main.py

## 📌 Usage
After running the program, choose from the menu:
- `add` → Add a new task
- `view` → View all tasks
- `update` → Update an existing task
- `delete` → Delete a task
- `exit` → Exit the program

Tasks are stored in a `tasks.json` file.

## 🎯 Project Goal
This project is part of the **roadmap.sh Python Projects** and focuses on:
- Clean code structure
- Input validation
- File handling
- CLI-based interaction

## 🔗 Project Page

This project is based on the roadmap.sh Task Tracker project:

https://roadmap.sh/projects/task-tracker
